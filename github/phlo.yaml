# Phlo Project Configuration
name: github-stats
description: "github-stats data lakehouse"

infrastructure:
  services:
    minio:
      service_name: minio
      api_port: 9100
      console_port: 9101
